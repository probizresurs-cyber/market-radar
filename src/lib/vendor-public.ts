/**
 * Публичные реквизиты и контакты исполнителя — то, что показывается посетителю.
 *
 * Отдельно от `requisites.ts` намеренно: там серверная функция, читающая
 * process.env (включая банковские реквизиты для счетов), а лендинги — клиентские
 * компоненты, куда банковские данные не должны попадать вообще. Здесь только то,
 * что и так напечатано в оферте на сайте.
 *
 * Зачем это на лендинге: человек приходит с платной рекламы на услугу за
 * десятки тысяч рублей и до сих пор видел единственный контакт — адрес почты.
 * Кому он платит — непонятно, и это резало конверсию на каждом шаге.
 */
export const VENDOR_PUBLIC = {
  legalName: "ИП Штумпф Юрий Геннадьевич",
  inn: "550615955642",
  ogrn: "317774600595262",
  address: "г. Москва, Шелепихинская наб., 34 к2, оф. 704, 123290",
  email: "support@marketradar24.ru",
  telegram: "https://t.me/market_radar1_bot",
  telegramLabel: "@market_radar1_bot",
  /**
   * Телефон показываем только если он реально заведён: пустая строка ничего не
   * рисует. Выдуманный номер на странице про честность — худшее, что можно
   * сделать. Задаётся через NEXT_PUBLIC_CONTACT_PHONE.
   */
  phone: process.env.NEXT_PUBLIC_CONTACT_PHONE ?? "",
} as const;

/**
 * Примеры разбора — настоящие ссылки на готовые документы.
 *
 * Сюда кладём ТОЛЬКО разборы наших собственных сайтов. Опубликовать аудит
 * чужой компании без её согласия нельзя, а «обезличенный кейс» — это
 * выдуманный кейс; на странице, где мы обещаем не выдумывать, такого быть
 * не должно. Пустой массив — блок не рисуется вовсе.
 */
export const DEMO_REPORTS: { title: string; note: string; href: string }[] = [];

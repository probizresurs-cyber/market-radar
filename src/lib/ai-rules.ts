/**
 * Общие правила для всех AI-промптов на платформе.
 *
 * Контекст: пользователи жаловались на галлюцинации — AI «уверенно» сочинял
 * конкретные числа (статистику, проценты роста, число сотрудников, годы
 * работы, рейтинги Яндекс.Карт), которые невозможно подтвердить публичными
 * данными. Это убивает доверие: владелец компании читает свой собственный
 * анализ и видит факты, которых нет.
 *
 * Решение: жёсткое правило для всех промптов — «не знаешь → не выдумывай».
 * Этот блок импортируется и склеивается с system/user prompt каждого
 * AI-роута (analyze-*, generate-*, content-factory).
 *
 * ВАЖНО: правило надо вписывать ДО самой задачи, чтобы AI его учитывал
 * с самого начала рассуждения. В конце промпта он его уже игнорирует.
 *
 * Использование:
 *   import { ANTI_HALLUCINATION_RULES } from "@/lib/ai-rules";
 *   const systemPrompt = `${ANTI_HALLUCINATION_RULES}\n\nТвоя задача: ...`;
 */

/**
 * Универсальный блок anti-hallucination — подходит и для analytical-промптов
 * (analyze-ta, analyze-smm, generate-swot), и для генеративных (generate-post,
 * generate-stories). В обоих случаях запрещено выдумывать конкретику.
 */
export const ANTI_HALLUCINATION_RULES = `═══ ПРАВИЛА AI-ГЕНЕРАЦИИ — ОБЯЗАТЕЛЬНО ═══

Этот контент видит реальный владелец бизнеса или потенциальный клиент.
Любая выдуманная конкретика разрушает доверие. Поэтому:

ЗАПРЕЩЕНО ВЫДУМЫВАТЬ:
- Статистику и проценты («95% довольны», «теряете 30% трафика», «средний CTR 5%»)
- Конкретные цифры компании: число сотрудников, оборот, годы работы, число клиентов
- Рейтинги на Яндекс.Картах / Google / 2GIS — если они не переданы во входных данных
- Число отзывов, упоминаний в СМИ, наград, дипломов
- Цены, скидки, акции, сроки доставки
- Имена сотрудников, регалии, опыт, образование
- Кейсы клиентов («Анна обратилась к нам», «компания X выросла на N%»)
- Цитаты и отзывы клиентов
- Конкретные технологии, бренды-партнёры, оборудование
- Географию (число филиалов, города присутствия)
- Конкурентов: если не знаешь реально существующие домены в нише — лучше пусто

КАК ПИСАТЬ КОГДА ДАННЫХ НЕТ:
- Качественные характеристики вместо чисел: «есть потенциал роста», «команда профессионалов»
- Гипотезы помечай словами: «скорее всего», «вероятно», «есть основания полагать», «обычно»
- Можно дать общие наблюдения для ниши («стоматологии в Москве обычно...») если это правда
- Лучше короче и честнее, чем подробнее и с выдумкой
- Если в задаче явно нужна цифра, а ты её не знаешь — верни null или явно «недостаточно данных»

═══ КОНЕЦ ПРАВИЛ ═══`;

/**
 * Сокращённая версия для случаев когда нужна компактность (system_prompt
 * с тяжёлым существующим текстом). Покрывает то же самое в 3 строки.
 */
export const ANTI_HALLUCINATION_SHORT = `КРИТИЧНО: НЕ ВЫДУМЫВАЙ конкретные числа (статистику, проценты, рейтинги, оборот, число клиентов, годы работы) которых нет во входных данных. НЕ выдумывай имена/кейсы/цитаты клиентов. Не знаешь — пиши абстрактно или явно «недостаточно данных». Гипотезы помечай «скорее всего», «вероятно».`;

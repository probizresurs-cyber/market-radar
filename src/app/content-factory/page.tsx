"use client";

import AppShell from "@/components/AppShell";

// Продукт «Контент-завод» — посты, сторис, видео, карусели, план, календарь.
export default function Page() {
  return <AppShell scope="content-factory" />;
}

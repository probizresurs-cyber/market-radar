"use client";

/**
 * /new2 — полная версия страницы диагностики.
 *
 * Сюда переехало то, что раньше лежало на /new: все маркетинговые разделы
 * (где теряются заявки, два пути к клиенту, что мы делаем, что получает
 * клиент, кто исполнитель). На /new теперь короткий мини-лендинг под
 * рекламу, а этот адрес остаётся для тех, кому нужна длинная аргументация:
 * ссылки из писем, переписки с клиентом, ручные отправки.
 *
 * Тот же компонент, что и на /check, без compact.
 */
export { default } from "../check/page";

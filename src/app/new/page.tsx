"use client";

/**
 * /new — мини-лендинг под рекламу, канонический вход воронки.
 *
 * Короткий и запоминающийся адрес для объявлений: marketradar24.ru/new.
 * Раньше здесь лежала полная страница со всеми маркетинговыми разделами —
 * она переехала на /new2, а сюда встал компактный вариант: первый экран,
 * форма, результаты замеров прямо на месте и захват почты.
 *
 * Что осталось из длинной версии и почему именно это:
 *   — разбор частых обещаний: единственный блок, который отличает нас от
 *     соседей по выдаче, и снимает главное возражение «все обещают одно»;
 *   — цена «от 25 000 ₽/мес»: единственная цифра денег, без неё человек
 *     доходит до разбора, не понимая порядка сумм;
 *   — пример настоящего разбора: доказательство, что документ существует;
 *   — реквизиты в подвале: по 152-ФЗ и для модерации Директа.
 *
 * Технически это тот же компонент, что и на /check и /new2, с compact=true.
 * Второй реализации воронки не заводим: расхождение между коротким и длинным
 * входом означало бы, что правку надо вносить дважды.
 */
import { CheckFunnel } from "../check/page";

export default function NewPage() {
  return <CheckFunnel compact />;
}

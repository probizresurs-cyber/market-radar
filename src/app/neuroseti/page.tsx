"use client";

/**
 * /нейросети — мини-лендинг под рекламу, основной вход воронки.
 *
 * Кириллический адрес выбран намеренно: «маркетрадар24.ру слэш нейросети»
 * диктуется по телефону и читается в объявлении, в отличие от /new, который
 * ничего не значил. Плата за это — процентное кодирование при копировании
 * ссылки (/%D0%BD%D0%B5%D0%B9...), поэтому в письмах и документах лучше
 * давать ссылку кликом, а не текстом.
 *
 * Прежний /new остаётся рабочим и ведёт сюда постоянным редиректом:
 * объявления, уже запущенные на него, не должны сломаться.
 *
 * Технически это тот же компонент воронки с compact=true, что и на /check
 * и /new2, а не отдельная страница: расхождение между короткой и длинной
 * версией означало бы, что правку надо вносить дважды.
 */
import { CheckFunnel } from "../check/page";

export default function NeuroPage() {
  return <CheckFunnel compact />;
}
